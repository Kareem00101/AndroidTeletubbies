<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.teletubbies_task_4">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/google_play_movie"
        android:label="Teletubbies"
        android:roundIcon="@drawable/google_play_movie"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".UI.MovieDetailsActivity"></activity>
        <activity android:name=".UI.MainActivity2"></activity>
        <activity android:name=".UI.MainActivity"></activity>
        <activity android:name=".UI.RecyclerViewMovies" />
        <activity
            android:name=".UI.StartScreen"
            android:theme="@style/SplashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>